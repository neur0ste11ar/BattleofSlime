#pragma once

using namespace std;

enum Type { attack, heal, weather, sing, paint };
enum Element { normal, grass, fire, water, electric };
enum Status { asleep, painted };
enum Weather { common, sun, rain };
enum Action { escape, skill, change, potion };
enum Result { success, failure, waste };